# CinaVault Premium — 65-Second Promotional Package

This directory contains the finalized **65-second 16:9 Hollywood-style CinaVault Premium advertisement**. The package includes the rendered MP4, English male narration, shot blueprint, deterministic FFmpeg assembly script, asset-provenance note, and checksum manifest.

The production preserves tracked CinaVault logo and banner assets. Because the source checkout did not contain a complete UI screen-capture library, the metadata, sidecar, playback, repair, remote-access, and scale sequences are explicitly styled feature visualizations, not literal application screenshots. Claims are constrained to the accompanying blueprint and the repository build summaries.

Verify the rendered MP4 and WAV from this directory with `sha256sum -c cinavault-premium-65sec-promo-sha256.txt`. The assembly script expects the authoritative logo and banner under `public/branding/` and the generated holographic source frames at the documented local paths.

The public product reference is [CinaVault Premium on GitHub](https://github.com/johngraven75/CinaVault-Premium).
