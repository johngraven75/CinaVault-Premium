# This workflow uses actions that are not certified by GitHub.
# DISABLED: The apisec-inc/ethicalcheck-action is not available
# TODO: Configure proper API security testing with maintained action

name: EthicalCheck-Workflow

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow disabled
        run: echo "EthicalCheck workflow is disabled - action not available. Configure proper API security testing."
