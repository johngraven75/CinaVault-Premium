# APIsec workflow - DISABLED
# This workflow requires APIsec.ai account and GitHub secrets configuration
# See: https://www.apisec.ai/ for setup instructions

name: APIsec

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow disabled
        run: echo "APIsec workflow is disabled - requires configuration. See workflow comments for setup instructions."
